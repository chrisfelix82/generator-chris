define([],function(){
	
	/**
	 * This is just a sample module that simulates shared memory between views.  In this case, simply an array called items
	 * @module commonapp/Store
	 */
    return {
    	
    	/**
    	 * An array of items that will be rendered in the master list of HomeView
    	 * @type {string[]}
    	 */
        items : []
    };
});